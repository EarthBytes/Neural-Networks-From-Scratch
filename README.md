# Neural Networks From Scratch

From-scratch NumPy neural network experiments (XOR, synthetic regression, Iris).

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
