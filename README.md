# Neural Networks From Scratch

From-scratch NumPy neural network experiments (XOR, synthetic regression, Iris).

Scaffolding in progress — see `build_instructions.md` and `project_plan.md`.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
pip install -r requirements.txt
```
